/*import background from '../../assets/backgrounds/book.jpg'

import {
  Id,
  Name,
  Title,
  ImageContainer,
  Image,
  Container
} from './Card.style';

const Card = ({
  pokemon: { id, name, title, imageUrl },
  ...rest
}) => {
  return (
    <Container {...rest}>
      <ImageContainer>
          <Image src={imageUrl} width={82} height={122} alt={title} />
      </ImageContainer>
      <div className="content">
        <Title>{title}</Title>
        <Id>#{id}</Id>
        <Name>{name}</Name>
      </div>
    </Container>
  )
}

//export default Card;*/